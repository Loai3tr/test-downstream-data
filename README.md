# test-downstream-data
Pre-processed RNA-seq test data for downstream analysis pipeline validation. Contains count matrices, tximport outputs, edgeR/voom objects, and sample metadata for testing differential expression modules.
